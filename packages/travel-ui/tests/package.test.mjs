import test from "node:test"; import assert from "node:assert/strict"; import { readFile } from "node:fs/promises";
test("travel frontend is separately importable and has no bundled fake trip", async()=>{ const pkg=JSON.parse(await readFile(new URL("../package.json",import.meta.url),"utf8")); assert.ok(pkg.exports["."]); const source=await readFile(new URL("../src/index.tsx",import.meta.url),"utf8"); assert.match(source,/不会用示例行程冒充/); assert.doesNotMatch(source,/api[_-]?key|Authorization/i); });
