// The complete animation and finished vectors remain the single visual source in
// the public shell. The package build bundles that source into a standalone npm
// artifact so consumers can install only the ambience layer.
export { AmbientLines, LineEffectGlyph } from "../../../apps/web/src/ambient-lines";
export type { LineEffect } from "../../../apps/web/src/ambient-lines";
