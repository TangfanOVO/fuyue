# 公开包边界

本文件定义什么可以进入赴约公开仓。违反任一“不得公开”规则时，发布必须停止。

发布打包和两条 GitHub 构建工作流都会在缺少私有仓密钥
`PUBLIC_PRIVATE_MARKERS` 时失败关闭。它应当是逗号分隔的私人名字、昵称、
房间名和其他绝不得进入公开包的短语。普通克隆与本地测试不需要这个密钥；
只有发布和打包闸门必须带上它。

## 可以公开

- 通用 Web / PWA 源码
- 与 PWA 共用前端的 Android Capacitor 容器、空白图标与无签名调试构建配置
- 通用数据类型、LocalData 实现与迁移代码
- 空白人物资料和专门编写的演示文本
- 不含真实对话的测试夹具
- 模型适配器接口与用户自建 relay 契约
- 架构、开发、部署和恢复文档
- 不含个人标识的截图与媒体

## 不得公开

- 真实姓名、私人关系资料、私人身份文件
- 真实聊天、信件、日记、记忆、收藏与时间线
- 从真实资料脱敏得到的演示数据
- 数据库、备份、导出包、日志、会话文件与 source map
- 图片、录音、附件和其他私人媒体
- `.env`、API Key、Token、Cookie、登录态、私钥与恢复密钥
- 私人域名、服务器地址、Basic Auth、数据库连接与运维入口
- 私有仓名称、内部主机别名和仅供家庭使用的功能配置

## 结构规则

`open-source/` 必须能独立安装、测试和构建。公开发布只能从这个目录生成，不得把私密总仓直接改为 Public。

公开能力以 `packages/core/src/capabilities.ts` 与 `fuyue.layers.json` 的现有条目为准；发布脚本只复制清单允许的文件，不从其他工作区推测或补齐功能。

通用数据结构可以声明 `chatgpt_work`、`codex` 等来源类型，但测试数据不得来自真实会话。真实导入只发生在使用者自己的浏览器或自托管实例中。

公开版只提供可见、可导出的 LocalData：普通聊天按 48 小时窗口取原文，电话只带本通最近 8 轮；额外数据源必须由使用者在自己的后端明确实现、说明和授权。

## 发布前检查

1. 在全新临时目录中只复制 `open-source/`。
2. 使用 `PUBLIC_PRIVATE_MARKERS` 传入当前个人姓名、域名、仓名和主机别名，运行 `npm run check:privacy`。
3. 运行凭据扫描和 Git 历史扫描。
4. 运行 typecheck、测试与生产构建。
5. 使用新浏览器创建人物、记录聊天、创建记忆、导出副本。
6. 人工审阅公开文件清单和页面截图。
7. 得到所有者明确确认后才推送到公开仓。
